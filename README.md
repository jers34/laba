# FriendConnect

A lightweight website for making friends with simple privacy controls. Users can register, log in, and add friends with either public or private visibility.

## Usage
Open `index.html` in a browser to use the app. Friends and users are stored in the browser's local storage.

## Scripts
- `npm test` – placeholder test script.
